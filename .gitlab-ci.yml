stages:
  - deploy

deploy:
  stage: deploy
  tags:
    - production  # Correspond au tag du runner Shell
  script:
    - echo "🚀 Déploiement en cours..."
    # Exemple de commandes Shell (remplacez par vos besoins)
    - ls -la
    - docker compose down
    - docker compose -f docker-compose.prod.yml up -d --build
  only:
    - develop